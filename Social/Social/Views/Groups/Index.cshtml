@model IEnumerable<Social.Group>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Universe.origin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.groupName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ParentOrganisation)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Universe.origin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.groupName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ParentOrganisation)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.groupID }) |
            @Html.ActionLink("Details", "Details", new { id=item.groupID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.groupID })
        </td>
    </tr>
}

</table>
